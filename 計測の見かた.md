# 計測の見かた

## 何を数えているか
- `/api/ev` … ページ表示・ページ内再生（データセット **maiki_events**）
- `/go/<slug>` … 外部リンクのクリック（データセット **maiki_clicks**）

個人を識別する値は送っていない。イベント名・どこから押されたか・国だけ。

## 動いているかの確認
```
curl -s "https://mikey-master.pages.dev/api/ev?e=__diag"
```
`{"EVENTS":true,"CLICKS":true}` が返れば本番に計測が繋がっている。
`false` が出たら Cloudflare Pages → mikey-master → 設定 → バインディングを確認し、
**そのあと必ず再デプロイする**（バインディングは次のデプロイから効く）。

## データの読み方
```
./計測を見る.sh        # 直近7日
./計測を見る.sh 30     # 直近30日
```

Analytics Engine はダッシュボードに一覧画面が無く、SQL API でしか読めない。
APIトークン（権限: **アカウント / Account Analytics / 読み取り**）が要る。
作ったら `~/.config/cloudflare/maiki.env` の `CF_ANALYTICS_TOKEN=` に貼る。
⚠️ このファイルはリポジトリに入れない（chmod 600 済み）。

生のSQLを叩く場合:

```
curl -s https://api.cloudflare.com/client/v4/accounts/<ACCOUNT_ID>/analytics_engine/sql \
  -H "Authorization: Bearer <TOKEN>" \
  -d "SELECT blob1 AS detail, index1 AS ev, sum(_sample_interval) AS n
      FROM maiki_events WHERE timestamp > NOW() - INTERVAL '7' DAY
      GROUP BY ev, detail ORDER BY n DESC"
```

クリック側:
```
  SELECT index1 AS slug, blob1 AS src, sum(_sample_interval) AS n
  FROM maiki_clicks WHERE timestamp > NOW() - INTERVAL '7' DAY
  GROUP BY slug, src ORDER BY n DESC
```

⚠️ データの保持は 90日。それ以上遡れない。
⚠️ `count()` ではなく `sum(_sample_interval)` を使う。サンプリングされた分を復元するため。
